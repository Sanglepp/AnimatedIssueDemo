import { ViewStyle } from "react-native";

const FULL_WIDTH: ViewStyle = {
  width: '100%',
}

const ROW: ViewStyle = {
  flexDirection: 'row',
}

const CENTER_ALIGN: ViewStyle = {
  alignItems: 'center',
}

export default {
  ROW,
  CENTER_ALIGN,
  FULL_WIDTH,
}